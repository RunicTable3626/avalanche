services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: avalanche
      POSTGRES_USER: avalanche
      POSTGRES_PASSWORD: avalanche-dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ../infra/migrations:/docker-entrypoint-initdb.d:ro

volumes:
  pgdata:
